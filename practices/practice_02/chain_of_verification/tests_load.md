# Нагрузочные проверки

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Пиковые обращения к POST /api/reviews: `create_review(payload)` передаёт `payload["diff"]` в `review_service.review`, который вызывает внешний `self.llm.generate(prompt)` (TRAINING_PR.diff:35-37, 19-22); «пиковые» — допущение | 50 rps в течение 60 секунд, LLM заменён на быстрый мок (допущение); для реального LLM-вызова — таймаут 10 секунд (REL-1, CASE.md) | Средняя задержка ≤ 500 мс (допущение); ошибки ≤ 1% (допущение); diff длиннее 20 000 символов — HTTP 413 (API-1, CASE.md) | Время ответа API (из логируемой длительности) и доля 4xx (HTTP 413) | Лог сервера по OBS-1: только `request_id`, длительность, статус (CASE.md) |

Если нагрузочное тестирование пока не требуется, обоснуйте это и назовите условие, после которого оно понадобится.

## Как использовали AI

- Строка в [`../prompts.md`](../prompts.md): Chain of Verification
- Что проверили и исправили сами: по источникам CASE.md и TRAINING_PR.diff подтверждены POST /api/reviews, вызов внешнего LLM, правила REL-1 (таймаут 10 с) и API-1 (HTTP 413); нагрузка, длительность и пределы без источника помечены «допущение»; трейсинг и доля 5xx без источника убраны