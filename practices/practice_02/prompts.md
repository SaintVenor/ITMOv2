# Журнал экспериментов Практики 2

- Выбранный слабый артефакт Практики 1:
- practices/practice_01/tests_load.md — таблица «Нагрузочные проверки»
- Что в нём нужно улучшить:
- Сделать цели нагрузки измеримыми и проверяемыми по доступным источникам (CASE.md, TRAINING_PR.diff), добавить воспроизводимость.
- Как поймём, что изменение полезно:
- Строка сценария содержит: ссылку на код эндпоинта, измеримые цели (p99, 100% 200), источник evidence в рамках OBS-1, команду запуска. Ревьюер может повторить проверку.

| Техника | Файл эксперимента | Изменённый файл Практики 1 | Конкретное изменение | Проверка | Что отклонили |
|---|---|---|---|---|---|
| Few-shot | [`few_shot/experiment.md`](few_shot/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` → исправленная версия: [`few_shot/tests_load.md`](few_shot/tests_load.md) | Уточнили эндпоинт и метод; заменили среднее на p99; цели: 100% 200; evidence привязан к OBS-1; добавили команду запуска | Проверка через CASE.md (OBS-1), TRAINING_PR.diff (`@app.post("/api/reviews")`), и наличие команды запуска в few_shot/tests_load.md | Отклонены Grafana/APM и допущения о трафике без пометки |
| R.C.T.F. | [`rctf/experiment.md`](rctf/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` → исправленная версия: [`rctf/tests_load.md`](rctf/tests_load.md) | Сценарий уточнён до POST /api/reviews (TRAINING_PR.diff:35); числа 50 rps / 60 с / ≤500 мс / ≤1% помечены «допущение»; внесены подтверждённые пределы REL-1 (timeout LLM 10 с, CASE.md:66) и API-1 (HTTP 413 для diff > 20 000 символов, CASE.md:65) с отдельными сценариями; «доля 4xx/5xx» заменена на статус-коды, доля 413 выделена отдельно; «метрики сервера и трейсинг» убраны; evidence ограничен OBS-1 (request_id, длительность, статус); шаблонная строка заменена обоснованием и условием | Повторная проверка — grep -F по указанным файлам: `@app.post("/api/reviews")` (TRAINING_PR.diff:35), `answer = self.llm.generate(prompt)` (TRAINING_PR.diff:21); в CASE.md `diff длиннее 20 000 символов отклоняется с HTTP 413.` (:65), `внешний LLM-вызов имеет timeout 10 секунд` (:66), `в лог пишутся только request_id` (:70); в rctf/tests_load.md каждое число без источника помечено «допущение», нет слова «трейсинг», есть ссылка на ../prompts.md | Отклонены: «метрики сервера и трейсинг» как evidence (противоречит OBS-1); «доля 4xx/5xx» (413 легитимен по API-1, «доля 5xx» без источника); «быстрый мок LLM» без ограничения (мешает проверить REL-1); строка P1-03 и утверждение «согласовали таймаут и 413» как неподтверждённые таблицей; числа 50 rps / 60 с / ≤500 мс / ≤1% — только с пометкой «допущение» |
| Chain of Verification | [`chain_of_verification/experiment.md`](chain_of_verification/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` → исправленная версия: [`chain_of_verification/tests_load.md`](chain_of_verification/tests_load.md) | Подтверждены по источникам: POST /api/reviews и передача payload["diff"] в review_service.review (TRAINING_PR.diff:35-37), вызов внешнего LLM с diff в промпте (TRAINING_PR.diff:19-22), REL-1 (таймаут 10 с) и API-1 (HTTP 413) внесены в сценарий и предел, evidence ограничен OBS-1 (request_id, длительность, статус); 50 rps / 60 с / мок / ≤500 мс / ≤1% помечены «допущение»; трейсинг и 5xx без источника убраны | Повторная проверка — grep -F по указанным файлам: `@app.post("/api/reviews")` в TRAINING_PR.diff; `answer = self.llm.generate(prompt)` в TRAINING_PR.diff; `diff длиннее 20 000 символов отклоняется с HTTP 413.` в CASE.md; `внешний LLM-вызов имеет timeout 10 секунд` в CASE.md; `в лог пишутся только request_id, длительность и статус.` в CASE.md | Трейсинг и доля 5xx без источника; числа и инструмент без источника — только с пометкой «допущение»; формулировка «согласовали» без явного внесения REL-1/API-1 |
| Tree of Thoughts | [`tree_of_thoughts/experiment.md`](tree_of_thoughts/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` → исправленная версия: [`tree_of_thoughts/tests_load.md`](tree_of_thoughts/tests_load.md) | По выбранной реализации C2 таблица заменена абзацем «Если нагрузочное тестирование пока не требуется…»: обоснование — в CASE.md/TRAINING_PR.diff нет целевого RPS и SLO, только REL-1 (таймаут 10 с) и API-1 (HTTP 413); условие — появится требование к нагрузке или реальный трафик; зафиксирован план будущего замера в рамках OBS-1; «трейсинг» из evidence удалён; числа 50 rps / ≤500 мс / ≤1% изъяты или помечены «допущение» | Повторная проверка — grep по источникам: `@app.post("/api/reviews")` и `payload["diff"]` в TRAINING_PR.diff:35-37; в CASE.md «timeout 10 секунд» (REL-1), «HTTP 413» (API-1), «request_id, длительность и статус» (OBS-1); в обоих источниках нет целевого RPS/SLO для /api/reviews; в tree_of_thoughts/tests_load.md заполненный абзац вместо таблицы, числа-допущения помечены, в «Как использовали AI» есть ссылка на ../prompts.md | Отклонены: B (быстрый мок — отсечена по профильному К1: искажает картину LLM-вызова по REL-1), A (реальный LLM-прогон сейчас — К4=1, нет целевой нагрузки в источниках), реализации A1 (9), A2 (10), C1 (10) — ниже C2 (11) |
| RAG | [`rag/experiment.md`](rag/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` → исправленная версия: [`rag/tests_load.md`](rag/tests_load.md) | Сценарий уточнён до POST /api/reviews (TRAINING_PR.diff:35-37, payload["diff"]); из CASE.md внесены подтверждённые пределы API-1 (HTTP 413 для diff > 20 000 символов) и REL-1 (timeout 10 с) и их сценарии с целями из problem.md; 50 rps / 60 с / ≤500 мс / ≤1% помечены «допущение, нет в источниках»; измерения — время ответа и статус-коды (REL-1), доля 413 по HTTP-логам API (API-1), «доля 5xx» убрана; evidence ограничен OBS-1 (request_id, длительность, статус); блок «Как использовали AI» ссылается на строку RAG | Повторная проверка — grep -F по указанным файлам: `@app.post("/api/reviews")` и `payload["diff"]` в TRAINING_PR.diff:35-37; `diff длиннее 20 000 символов отклоняется с HTTP 413.` (CASE.md:65), `внешний LLM-вызов имеет timeout 10 секунд` (CASE.md:66), `в лог пишутся только request_id` (CASE.md:70); `Доля запросов с задержкой LLM >10с` и `Доля запросов с diff > 20000 символов` в problem.md; наличие rag/tests_load.md и ссылки на ../prompts.md | Отклонены: «метрики сервера и трейсинг» как evidence (противоречит OBS-1), «доля 5xx» (фрагмента нет); числа 50 rps / 60 с / ≤500 мс / ≤1% — только с пометкой «допущение, нет в источниках»
| ReAct | [`react/experiment.md`](react/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` → исправленная версия: [`react/tests_load.md`](react/tests_load.md) | Цикл действие → наблюдение → решение: по шагам сверили сценарий с `problem.md`, `tests_integration.md`, `tests_e2e.md`, `adr.md`, `CASE.md` и `TRAINING_PR.diff`; заменили один нецелевой сценарий («средняя задержка ≤ 500 мс, ошибки ≤ 1%», evidence «метрики сервера и трейсинг») на четыре нагрузочные проверки метрик SEC-1/API-1/REL-1/OUT-1 с целями из `problem.md` и пределами из `CASE.md`; evidence ограничен OBS-1 (журнал мок-LLM, `request_id`, статус, длительность); шаблонную строку про «если не требуется» заменили обоснованием; числа 50 rps/60 с/доля смеси помечены «допущение»; в «Как использовали AI» — ссылка на строку ReAct в `../prompts.md` | Повторяемо: `grep -F` по `TRAINING_PR.diff` (`@app.post("/api/reviews")`, `payload["diff"]`), `CASE.md` (`HTTP 413`, `timeout 10 секунд`, `request_id`), `problem.md` (цели `0% → 100%`, `<50% → 100%`); проверка отсутствия «трейсинг»/«5xx» и наличия ссылки на `../prompts.md` в `react/tests_load.md` | Отклонены: «трейсинг» и «доля 4xx/5xx» (противоречат OBS-1 и искажают API-1: 413 — легитимный статус); «ошибки ≤ 1%» без источника и без метрики в `problem.md`; быстрый мок LLM для REL-1 (не измеряет таймаут 10 с); целевой RPS/SLO как факт; числа 50 rps/60 с/500 мс/1% — только с пометкой «допущение» |

## Независимое ревью

| Замечание другой команды | Где исправили | Evidence |
|---|---|---|
| Двусмысленность |  |  |
| Непроверяемое требование |  |  |
| Пропущенный риск или источник |  |  |
