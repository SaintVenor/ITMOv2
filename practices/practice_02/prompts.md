# Журнал экспериментов Практики 2

- Выбранный слабый артефакт Практики 1:
- practices/practice_01/tests_load.md — таблица «Нагрузочные проверки»
- Что в нём нужно улучшить:
- Сделать цели нагрузки измеримыми и проверяемыми по доступным источникам (CASE.md, TRAINING_PR.diff), добавить воспроизводимость.
- Как поймём, что изменение полезно:
- Строка сценария содержит: ссылку на код эндпоинта, измеримые цели (p99, 100% 200), источник evidence в рамках OBS-1, команду запуска. Ревьюер может повторить проверку.

| Техника | Файл эксперимента | Изменённый файл Практики 1 | Конкретное изменение | Проверка | Что отклонили |
|---|---|---|---|---|---|
| Few-shot | [`few_shot/experiment.md`](few_shot/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` (итоговая таблица); вклад варианта как evidence: [`few_shot/tests_load.md`](few_shot/tests_load.md) | Вошло в итоговый файл: p99 задержки вместо средней (критика Few-shot: среднее скрывает хвост) и команда запуска `k6 run load/reviews.js` (допущение: k6) в evidence строки «пиковые». Не вошло: цель «100% ответов 200» — под нагрузкой легитимны 413 (API-1, CASE.md:65), поэтому в итоге допущение о доле ошибок; один сценарий без REL-1/API-1 — в итоге эти пределы отдельными строками | Повторить из корня репозитория: `grep -F '@app.post("/api/reviews")' practices/practice_01/TRAINING_PR.diff` → строка 35; `grep -F 'k6 run load/reviews.js' practices/practice_01/tests_load.md` → команда запуска; `grep -F 'diff длиннее 20 000 символов отклоняется с HTTP 413.' practices/practice_01/CASE.md` → строка 65 (API-1); `grep -F 'внешний LLM-вызов имеет timeout 10 секунд' practices/practice_01/CASE.md` → строка 66 (REL-1); `grep -F 'в лог пишутся только' practices/practice_01/CASE.md` → строка 70 (OBS-1) | Отклонены: «100% ответов 200» как абсолютная цель (413 легитимен — CASE.md:65); p99 ≤ 500 мс и 50 rps/60 с без пометки «допущение»; инструмент k6 — только как «допущение»; один сценарий вместо трёх (нет REL-1/API-1) |
| R.C.T.F. | [`rctf/experiment.md`](rctf/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` (итоговая таблица); вклад варианта как evidence: [`rctf/tests_load.md`](rctf/tests_load.md) | Вошло в итоговый файл: набор из трёх сценариев (пиковые обращения, таймаут внешнего LLM по REL-1, отклонение большого diff по API-1); пометки «— допущение, в источниках нет» для 50 rps/60 с/500 мс/1%; статус-коды и доля 413 вместо «доли 4xx/5xx»; evidence только по OBS-1 (request_id, длительность, статус); обоснование про отсутствие целевого RPS/SLO сохранено одной строкой под таблицей. Не вошло: пределы REL-1/API-1 внутри строки «пиковые» — вынесены отдельными строками без дублирования; шаблонная строка «Если нагрузочное тестирование…» осталась на месте (вошло только ограничение-обоснование) | `grep -F '@app.post("/api/reviews")' practices/practice_01/TRAINING_PR.diff` → :35; `grep -F 'answer = self.llm.generate(prompt)' practices/practice_01/TRAINING_PR.diff` → :21; `grep -F 'diff длиннее 20 000 символов отклоняется с HTTP 413.' practices/practice_01/CASE.md` → :65; `grep -F 'внешний LLM-вызов имеет timeout 10 секунд' practices/practice_01/CASE.md` → :66; `grep -F 'в лог пишутся только' practices/practice_01/CASE.md` → :70; в `../practice_01/tests_load.md` каждое число без источника содержит «допущение», нет слов «трейсинг» и «доля 4xx/5xx» | Отклонены: «метрики сервера и трейсинг» как evidence (противоречит OBS-1 — CASE.md:70); «доля 4xx/5xx» в прежнем виде (413 — легитимный статус по API-1, «доля 5xx» осталась только допущением); «быстрый мок» как полноценная замена LLM для REL-1 (не воспроизводит задержку > 10 с — CASE.md:66); 50 rps/60 с/500 мс/1% без пометки «допущение» |
| Chain of Verification | [`chain_of_verification/experiment.md`](chain_of_verification/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` (итоговая таблица); вклад варианта как evidence: [`chain_of_verification/tests_load.md`](chain_of_verification/tests_load.md) | Вошло в итоговый файл: подтверждённые цитатами привязки сценария — POST /api/reviews, передача `payload["diff"]` в `review_service.review`, вызов `self.llm.generate(prompt)` (TRAINING_PR.diff:35-37, 19-22); «пиковые» как «допущение»; время ответа из логируемой длительности; evidence по OBS-1. Не вошло: подход CoV «таблица не растёт» — в итоге REL-1 и API-1 отдельными строками (пределы-допущения из единственной строки CoV не взяты как лучшая формулировка: p99 и команда запуска — из Few-shot, пометки — из R.C.T.F.) | `grep -F '@app.post("/api/reviews")' practices/practice_01/TRAINING_PR.diff` → :35; `grep -F 'return review_service.review(payload["diff"])' practices/practice_01/TRAINING_PR.diff` → :37; `grep -F 'answer = self.llm.generate(prompt)' practices/practice_01/TRAINING_PR.diff` → :21; `grep -F 'внешний LLM-вызов имеет timeout 10 секунд' practices/practice_01/CASE.md` → :66; `grep -F 'diff длиннее 20 000 символов отклоняется с HTTP 413.' practices/practice_01/CASE.md` → :65 | Отклонены: непроверенные числа исходника (500 мс, 1%) как пределы — в итог вошли только с пометкой «допущение»; компоновка без роста таблицы; «доля 4xx (HTTP 413)» как единственная метрика ошибок — в итоге сохранено допущение о 5xx |
| Tree of Thoughts | [`tree_of_thoughts/experiment.md`](tree_of_thoughts/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` (итоговая таблица); вклад варианта как evidence: [`tree_of_thoughts/tests_load.md`](tree_of_thoughts/tests_load.md) | Вошло в итоговый файл: обоснование реализации C2 сохранено одной строкой под таблицей — в CASE.md и TRAINING_PR.diff нет целевого RPS и SLO для /api/reviews, поэтому параметры нагрузки остаются допущениями; условие, после которого тест понадобится (появление целевых значений или реального трафика), — в той же строке; замечание о несовместимости «≤ 500 мс» с задержкой LLM до 10 с учтено в строке «пиковые». Не вошло: отказ от таблицы (решение принято заранее — Практика 1 требует заполненного раздела нагрузочных проверок); его обоснование сохранено как ограничение, а не как замена таблицы | Отсутствие целевой нагрузки в источниках: `grep -F '50 rps' practices/practice_01/CASE.md` и `grep -F '50 rps' practices/practice_01/TRAINING_PR.diff` → нет совпадений; `grep -F 'внешний LLM-вызов имеет timeout 10 секунд' practices/practice_01/CASE.md` → :66; `grep -F 'diff длиннее 20 000 символов отклоняется с HTTP 413.' practices/practice_01/CASE.md` → :65; `grep -F 'в лог пишутся только' practices/practice_01/CASE.md` → :70 | Отклонены: замена таблицы абзацем (принято заранее); прогон с реальным LLM (A) и быстрый мок как честная картина REL-1 (B) — не воспроизводит задержку > 10 с; числа 50 rps/60 с/500 мс/1% — только с пометкой «допущение» |
| RAG | [`rag/experiment.md`](rag/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` (итоговая таблица); вклад варианта как evidence: [`rag/tests_load.md`](rag/tests_load.md) | Вошло в итоговый файл: к пределу REL-1 добавлена цель метрики problem.md:19 (0% → 100%), к пределу API-1 — цель problem.md:18; перекрёстные ссылки на context.md (строки 19, 20, 24) рядом с правилами CASE.md; источник данных «HTTP-логи API» (problem.md:18) в evidence строки API-1; пометка «(мок-LLM в источниках есть только в метрике SEC-1, problem.md:17)» в строке «пиковые». Не вошло: сценарий SEC-1 как отдельная нагрузочная проверка — в итог вошла только пометка-допущение про мок-LLM (причины — в строке ReAct) | `grep -F 'Доля запросов с задержкой LLM >10с' practices/practice_01/problem.md` → :19; `grep -F 'Доля запросов с diff > 20000 символов' practices/practice_01/problem.md` → :18; `grep -F 'API-1: diff > 20 000' practices/practice_01/context.md` → :19; `grep -F 'REL-1: внешний LLM-вызов' practices/practice_01/context.md` → :20; `grep -F 'OBS-1: логируются только request_id' practices/practice_01/context.md` → :24; `grep -F 'HTTP-логи API' practices/practice_01/problem.md` → :18 | Отклонены: «метрики сервера и трейсинг» (противоречит OBS-1); «доля 5xx» (фрагмента нет — в итоге допущение); числа 50 rps/60 с/≤500 мс/≤1% — только с пометкой «допущение, в источниках нет»; сценарий SEC-1 как нагрузочный (маскирование — функциональное свойство, контекст измерения — problem.md:17 «На каждом релизе») |
| ReAct | [`react/experiment.md`](react/experiment.md) | Раздел «Нагрузочные проверки» в `../practice_01/tests_load.md` (итоговая таблица); вклад варианта как evidence: [`react/tests_load.md`](react/tests_load.md) | Вошло в итоговый файл: сквозная сверка с артефактами и подтверждение отсутствия в источниках чисел 50 rps/60 с/500 мс/1% — основание для пометки «допущение»; отказ от «трейсинга» и «доли 5xx»; связь пределов с целями problem.md. Не вошло: четыре сценария по SEC-1/API-1/REL-1/OUT-1 — в итоге REL-1 и API-1 отдельными строками, а SEC-1 и OUT-1 вне нагрузочного раздела: их контекст измерения в problem.md — юнит/интеграционные тесты и проверки на каждом релизе (строки 17, 20), а evidence OUT-1 «Снапшоты ответов» конфликтует со строгим прочтением OBS-1; вводные ссылки на tests_integration.md/tests_e2e.md/adr.md не вошли — они не входят в разрешённые источники цитат | `grep -F '@app.post("/api/reviews")' practices/practice_01/TRAINING_PR.diff` → :35; `grep -F 'внешний LLM-вызов имеет timeout 10 секунд' practices/practice_01/CASE.md` → :66; `grep -F 'diff длиннее 20 000 символов отклоняется с HTTP 413.' practices/practice_01/CASE.md` → :65; `grep -F 'в лог пишутся только' practices/practice_01/CASE.md` → :70; отсутствие в источниках: `grep -F '50 rps' practices/practice_01/CASE.md`, `grep -F '500 мс' practices/practice_01/CASE.md`, `grep -F '50 rps' practices/practice_01/problem.md` → нет совпадений; в `../practice_01/tests_load.md` нет «трейсинг»/«5xx» как источника evidence, числа без источника — только с «допущение» | Отклонены: нагрузочные сценарии SEC-1 и OUT-1 (контекст измерения — юнит/интеграция, problem.md:17, 20; evidence OUT-1 «снапшоты ответов» против OBS-1 — CASE.md:70); «доля 4xx/5xx» и «ошибки ≤ 1%» (413 легитимен — CASE.md:65, метрики «5xx» в problem.md нет); «быстрый мок» для REL-1 (не создаёт задержку > 10 с — CASE.md:66); «трейсинг» в любом виде; целевой RPS/SLO как факт |

## Независимое ревью

| Замечание другой команды | Где исправили | Evidence |
|---|---|---|
| Двусмысленность |  |  |
| Непроверяемое требование |  |  |
| Пропущенный риск или источник |  |  |
