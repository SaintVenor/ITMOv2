# Нагрузочные проверки

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Пиковые обращения к POST /api/reviews (TRAINING_PR.diff:35-38) | 50 rps (допущение) в течение 60 секунд (допущение), LLM заменён на быстрый мок (допущение) | p99 задержки ≤ 500 мс (допущение); HTTP 200 = 100% (допущение); 4xx/5xx = 0% (допущение) | Время ответа API (p99); распределение статусов | Ответы HTTP клиента и серверные логи по OBS-1 (request_id, duration, status) |
| Большой diff: проверка лимита 20 000 символов (API-1) для POST /api/reviews (TRAINING_PR.diff:35-38) | 5 rps (допущение) в течение 30 секунд (допущение), размер payload["diff"] > 20 000 символов | HTTP 413 = 100%; p99 задержки ≤ 200 мс (допущение) | Доля 413; p99 времени ответа | Ответы HTTP клиента; серверные логи по OBS-1 (duration, status); правило API-1 |
| Медленный LLM: обработка таймаута 10 с (REL-1) при POST /api/reviews; llm.generate с задержкой > 10 с (app/review_service.py:19-22) | 5 rps (допущение) в течение 30 секунд (допущение) | p99 задержки ≤ 11 с (допущение); 5xx = 0% (допущение); контролируемый ответ согласно REL-1 (допущение: HTTP 200) | Время ответа (p99); распределение статусов | Ответы HTTP клиента; серверные логи по OBS-1 (duration, status); правило REL-1; вызов llm.generate в TRAINING_PR.diff (app/review_service.py:19-22) |

Если нагрузочное тестирование пока не требуется, обоснуйте это и назовите условие, после которого оно понадобится.

## Как использовали AI

- Строка «R.C.T.F.» в [../prompts.md](../prompts.md)
- Что проверили и исправили сами: сверили метод и путь эндпоинта с TRAINING_PR.diff, добавили сценарии под API-1 и REL-1, заменили «среднюю» задержку на p99, привязали evidence к OBS-1, все ненайденные в источниках числа пометили как «допущение»
