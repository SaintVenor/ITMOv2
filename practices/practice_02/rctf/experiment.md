# R.C.T.F.

- **Role:**
- **Context:**
- **Task:**
- **Format:**

## Полный запрос


## Что получили


## Что изменили в исходном артефакте

- Файл и раздел:
- Изменение:
- Как проверили:
- Что отклонили:
Role: инженер по нагрузочному тестированию, ревьюер тест-планов сервиса AI-помощника ревью PR.

Context: тест-план practices/practice_01/tests_load.md сгенерирован моделью в Практике 1 и содержит неточности. Правила репозитория — practices/practice_01/CASE.md, код эндпоинта POST /api/reviews — practices/practice_01/TRAINING_PR.diff. Других источников фактов нет.

Task: найди в tests_load.md ошибки и неточности — в сценариях, числах, пределах, измерениях, evidence и в соответствии правилам репозитория, которые влияют на поведение сервиса под нагрузкой, — и составь исправленный вариант файла.

Format:
1. Исправленный вариант — новый файл practices/practice_02/rctf/tests_load.md: полная копия исходного с исправлениями, колонки таблицы те же. Каждый сценарий ссылается на правило или строку diff; каждый предел — измеримое значение; число без источника помечено «допущение». В блоке «Как использовали AI» — ссылка на строку R.C.T.F. в ../prompts.md.
2. Список ошибок: пронумерованный; у каждой — цитата из исходного файла, в чём ошибка, опора (правило или строка diff), исправление.
3. practices/practice_02/rctf/experiment.md: Role, Context, Task, Format и «Полный запрос» — дословно из этого сообщения; «Что получили» — список ошибок из п. 2; «Что изменили в исходном артефакте» — все четыре пункта, «Как проверили» — проверка, которую можно повторить.
4. practices/practice_02/prompts.md: все колонки строки R.C.T.F.; «Изменённый файл Практики 1» — раздел исходного ../practice_01/tests_load.md и ссылка на rctf/tests_load.md. Шапка заполняется, только если пуста. Чужие строки и таблица «Независимое ревью» не используются как источник и не меняются.

Полный запрос

Role: инженер по нагрузочному тестированию, ревьюер тест-планов сервиса AI-помощника ревью PR.

Context: тест-план practices/practice_01/tests_load.md сгенерирован моделью в Практике 1 и содержит неточности. Правила репозитория — practices/practice_01/CASE.md, код эндпоинта POST /api/reviews — practices/practice_01/TRAINING_PR.diff. Других источников фактов нет.

Task: найди в tests_load.md ошибки и неточности — в сценариях, числах, пределах, измерениях, evidence и в соответствии правилам репозитория, которые влияют на поведение сервиса под нагрузкой, — и составь исправленный вариант файла.

Format:
1. Исправленный вариант — новый файл practices/practice_02/rctf/tests_load.md: полная копия исходного с исправлениями, колонки таблицы те же. Каждый сценарий ссылается на правило или строку diff; каждый предел — измеримое значение; число без источника помечено «допущение». В блоке «Как использовали AI» — ссылка на строку R.C.T.F. в ../prompts.md.
2. Список ошибок: пронумерованный; у каждой — цитата из исходного файла, в чём ошибка, опора (правило или строка diff), исправление.
3. practices/practice_02/rctf/experiment.md: Role, Context, Task, Format и «Полный запрос» — дословно из этого сообщения; «Что получили» — список ошибок из п. 2; «Что изменили в исходном артефакте» — все четыре пункта, «Как проверили» — проверка, которую можно повторить.
4. practices/practice_02/prompts.md: все колонки строки R.C.T.F.; «Изменённый файл Практики 1» — раздел исходного ../practice_01/tests_load.md и ссылка на rctf/tests_load.md. Шапка заполняется, только если пуста. Чужие строки и таблица «Независимое ревью» не используются как источник и не меняются.

Что получили

1. «Пиковые обращения к /api/reviews | 50 rps в течение 60 секунд, LLM заменён на быстрый мок (допущение) | Средняя задержка ≤ 500 мс, ошибки ≤ 1% (допущение) | Время ответа API и доля 4xx/5xx | Метрики сервера и трейсинг» —
   - Ошибка: не указан метод эндпоинта; используется «средняя» задержка вместо p99; не задано требование к статусам по правилам репозитория; evidence («метрики сервера и трейсинг») противоречит OBS-1.
   - Опора: TRAINING_PR.diff:35-38 определяет POST /api/reviews; CASE.md: OUT-1, QA-1, OBS-1.
   - Исправление: указать POST /api/reviews с ссылкой на строки diff; заменить среднюю на p99; явно указать цель по статусам (допущение — 100% 200, 0% 4xx/5xx); evidence привязать к ответам HTTP клиента и серверным логам с полями OBS-1.

2. Отсутствует сценарий для лимита размера diff (API-1):
   - Ошибка: в план не включена проверка отклонения diff > 20 000 символов с HTTP 413.
   - Опора: CASE.md: API-1.
   - Исправление: добавить сценарий «Большой diff: проверка лимита 20 000 символов (API-1)» для POST /api/reviews, задать измеримые пределы: 100% 413; p99 задержки (допущение).

3. Отсутствует сценарий для таймаута внешнего LLM (REL-1):
   - Ошибка: нет проверки поведения при задержке llm.generate > 10 с и корректности контролируемого ответа.
   - Опора: CASE.md: REL-1; TRAINING_PR.diff: app/review_service.py:19-22 показывает вызов llm.generate.
   - Исправление: добавить сценарий «Медленный LLM: обработка таймаута 10 с (REL-1)» с измеримыми пределами p99 ≤ 11 с (допущение), 5xx = 0%, статус контролируемого ответа (допущение: 200).

4. Формулировка evidence в исходном сценарии: «Метрики сервера и трейсинг» —
   - Ошибка: истоки данных не согласуются с OBS-1; логировать содержимое diff и ответ модели нельзя.
   - Опора: CASE.md: OBS-1.
   - Исправление: уточнить evidence как «Ответы HTTP клиента и серверные логи по OBS-1 (request_id, duration, status)».

5. Неуказанные источники чисел:
   - Ошибка: числа нагрузки и пределов указаны без пометки источника/допущения.
   - Опора: Отсутствие таких чисел в CASE.md и TRAINING_PR.diff.
   - Исправление: все числа без источника явно пометить «допущение».

Что изменили в исходном артефакте

1. Уточнили метод и путь: POST /api/reviews, со ссылкой на TRAINING_PR.diff:35-38.
2. Заменили «среднюю» задержку на p99 и задали цели по статусам.
3. Добавили отдельные сценарии под API-1 (HTTP 413 при >20 000 символов) и REL-1 (таймаут 10 с внешнего LLM) с измеримыми пределами.
4. Evidence согласовали с OBS-1: только ответы HTTP клиента и логи с request_id, duration, status; убрали трейсинг.

Как проверили

- Сверили метод и путь эндпоинта с TRAINING_PR.diff:35-38.
- Проверили наличие и формулировки правил в CASE.md: API-1, REL-1, OBS-1, OUT-1, QA-1.
- Убедились, что каждый сценарий содержит ссылку на правило или строку diff; что каждый предел выражен измеримо; что все числа без источника отмечены как «допущение».
