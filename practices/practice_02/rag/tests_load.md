# Нагрузочные проверки

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Пиковые обращения к POST /api/reviews (TRAINING_PR.diff:35) | 50 rps в течение 60 секунд — допущение, нет в источниках; LLM заменён на быстрый мок — допущение, нет в источниках (мок-LLM в источниках упоминается только в метрике SEC-1: problem.md) | HTTP 413 для diff > 20 000 символов (CASE.md:65, API-1; context.md:19); контролируемый ответ при timeout LLM 10 секунд (CASE.md:66, REL-1; context.md:20); средняя задержка ≤ 500 мс и ошибки ≤ 1% — допущение, нет в источниках | Время ответа API и статус-коды (problem.md, метрика REL-1); доля 4xx/413 по HTTP-логам API (problem.md, метрика API-1); доля 5xx — допущение, нет в источниках | Логи в рамках OBS-1: request_id, длительность и статус (CASE.md:70, OBS-1; context.md:24) |
| Контролируемый таймаут внешнего LLM: задержка LLM > 10 секунд (CASE.md:66, REL-1; context.md:20) | Целевое rps и длительность — допущение, нет в источниках | 0% → 100% запросов с задержкой LLM > 10 с завершаются контролируемым ответом ≤ 10 с (problem.md, метрика REL-1; CASE.md:66) | Метрики времени ответа и статус-коды (problem.md, метрика REL-1) | Логи в рамках OBS-1: request_id, длительность и статус (CASE.md:70, OBS-1) |
| Отклонение большого diff: diff > 20 000 символов (CASE.md:65, API-1; context.md:19) | Целевое rps и длительность — допущение, нет в источниках | HTTP 413 для diff > 20 000 символов; 0% → 100% таких запросов получают 413 (CASE.md:65, API-1; problem.md, метрика API-1) | Доля запросов с diff > 20 000 символов, получивших 413 (problem.md, метрика API-1) | HTTP-логи API в рамках OBS-1: request_id, длительность и статус (problem.md, метрика API-1; CASE.md:70, OBS-1) |

Если нагрузочное тестирование пока не требуется, обоснуйте это и назовите условие, после которого оно понадобится.

## Как использовали AI

- Строка RAG в [`prompts.md`](../prompts.md): таблица журнала экспериментов, строка «RAG»
- Что проверили и исправили сами: из evidence убрали трейсинг и «метрики сервера» — трейсинг противоречит OBS-1 (CASE.md:70), evidence ограничен request_id, длительностью и статусом; убрали «долю 5xx» (фрагмента нет), оставили статус-коды и 413; числа 50 rps / 60 с / ≤500 мс / ≤1% пометили «допущение, нет в источниках»; в таблицу добавили сценарии из CASE.md: REL-1 (timeout 10 секунд) и API-1 (HTTP 413 для diff > 20 000 символов)