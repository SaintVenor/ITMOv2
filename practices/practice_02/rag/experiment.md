# RAG

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Вопрос к источникам:

Какие сценарии, нагрузка, пределы, измерения и evidence для нагрузочных проверок POST /api/reviews подтверждаются документами проекта и что в tests_load.md им не соответствует?

## Разрешённые источники

| Файл или документ | Зачем нужен | Какой фрагмент используем |
|---|---|---|
| practices/practice_01/CASE.md | правила репозитория: пределы API-1 и REL-1, ограничение логирования OBS-1 для evidence; других численных целей нагрузки нет | [F1] строка 65 (API-1), [F2] строка 66 (REL-1), [F4] строка 70 (OBS-1) |
| practices/practice_01/TRAINING_PR.diff | код эндпоинта POST /api/reviews и путь вызова внешнего LLM | [F5] строки 35–36, [F6] строка 37, [F7] строки 19–22 |
| practices/practice_01/problem.md | метрики успеха: целевые изменения и источники данных измерений | [F8] метрика API-1 (строка 18), [F9] метрика REL-1 (строка 19), [F10] метрика SEC-1 (строка 17, мок-LLM) |
| practices/practice_01/context.md | факты и ограничения проекта; подтверждение правил и описание текущего процесса | [F12] строка 19, [F13] строка 20, [F14] строка 24, [F15] строка 9, [F16] строка 10, [F17] строка 8 |

## Запрос

Роль: инженер по нагрузочному тестированию.

Работаешь методом RAG: сначала извлекаешь фрагменты из разрешённых источников, потом строишь ответ только из них. Утверждение без извлечённого фрагмента в ответ как факт не входит.

Объект проверки: practices/practice_01/tests_load.md. Это не источник — его утверждения проверяются по источникам.

Вопрос к источникам: какие сценарии, нагрузка, пределы, измерения и evidence для нагрузочных проверок POST /api/reviews подтверждаются документами проекта и что в tests_load.md им не соответствует?

Разрешённые источники (других файлов не открывай):
- practices/practice_01/CASE.md — правила репозитория;
- practices/practice_01/TRAINING_PR.diff — код эндпоинта;
- practices/practice_01/problem.md — метрики успеха;
- practices/practice_01/context.md — факты и ограничения проекта.

Шаг 1. Извлечение. Выпиши дословные фрагменты, относящиеся к вопросу. Каждому — идентификатор [F1], [F2]…, файл и место (строка или идентификатор правила). Фрагмент с пометкой «допущение» подтверждает только допущение, не факт; отметь это.

Шаг 2. Сопоставление. Каждое утверждение tests_load.md сопоставь с фрагментами: подтверждено [F#] / противоречит [F#] / фрагмента нет.

Шаг 3. Ответ. Собери исправленный вариант файла только из фрагментов. В каждой ячейке таблицы после утверждения — ссылка на источник в виде файла и места (например, `CASE.md`, `API-1`), чтобы файл читался без experiment.md. Утверждение без фрагмента помечай «допущение, нет в источниках». Противоречащее фрагментам убери.

Файлы:
- practices/practice_02/rag/tests_load.md — создай: полная копия исходного tests_load.md с исправлениями. В блоке «Как использовали AI» — ссылка на строку RAG в ../prompts.md.
- practices/practice_02/rag/experiment.md — заполни все поля: «Вопрос к источникам»; «Разрешённые источники» — строка на каждый файл: зачем нужен, какие фрагменты [F#] из него взяты; «Запрос» — дословно это сообщение; «Ответ со ссылками на источники» — фрагменты [F#] с цитатами, результат сопоставления из шага 2, затем таблица сценариев из исправленного файла; «Что изменили в исходном артефакте» — все четыре пункта, «Как проверили ссылки» — как повторить: поиск цитаты каждого [F#] через grep -F в указанном файле, «Что отклонили как неподтверждённое» — утверждения исходного файла без фрагмента или против фрагмента.
- practices/practice_02/prompts.md — заполни все колонки строки RAG. «Изменённый файл Практики 1» — раздел исходного ../practice_01/tests_load.md и ссылка на rag/tests_load.md. Шапку заполни, только если она пуста. Чужие строки и таблицу «Независимое ревью» не используй как источник и не меняй.

Исходный tests_load.md и остальные файлы не меняй, папки других техник не открывай, shell, git и сеть не используй.

Остановка: если источник нечитаем — сказать об этом и остановиться. Если фрагментов по нагрузке (rps, задержки, длительность) не нашлось — файл всё равно собери, пометив такие параметры «допущение, нет в источниках». Не создавай файл только если нечитаемы сами источники.

## Ответ со ссылками на источники

### Шаг 1. Извлечённые фрагменты

- [F1] `CASE.md`, строка 65 (правило API-1): «diff длиннее 20 000 символов отклоняется с HTTP 413.»
- [F2] `CASE.md`, строка 66 (правило REL-1): «внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в контролируемый ответ.»
- [F4] `CASE.md`, строка 70 (правило OBS-1): «в лог пишутся только request_id, длительность и статус. Содержимое diff и ответа модели не логируется.»
- [F5] `TRAINING_PR.diff`, строки 35–36: `@app.post("/api/reviews")` / `def create_review(payload: dict) -> dict[str, str]:`
- [F6] `TRAINING_PR.diff`, строка 37: `return review_service.review(payload["diff"])`
- [F7] `TRAINING_PR.diff`, строки 19–22: `def review(self, diff: str) -> dict[str, str]:` / `prompt = f"Review this pull request and find problems:\n{diff}"` / `answer = self.llm.generate(prompt)` / `return {"comment": answer}` — внешний LLM-вызов в пути запроса (подтверждает REL-1 как нагрузочный предел).
- [F8] `problem.md`, метрика API-1 (строка 18): «Доля запросов с diff > 20000 символов, получивших 413 | 0% → 100% | Интеграционные и e2e проверки | HTTP-логи API»
- [F9] `problem.md`, метрика REL-1 (строка 19): «Доля запросов с задержкой LLM >10с, завершившихся контролируемым ответом ≤10с | 0% → 100% | Интеграционные тесты с задержкой | Метрики времени ответа и статус-коды»
- [F10] `problem.md`, метрика SEC-1 (строка 17): «Доля запросов к LLM, где известные паттерны секретов отсутствуют в отправленном prompt (через мок-LLM)». Пометка: подтверждает только допущение про использование мок-LLM (для метрики SEC-1), к нагрузке не относится.
- [F12] `context.md`, строка 19: «API-1: diff > 20 000 символов отклоняется с HTTP 413»
- [F13] `context.md`, строка 20: «REL-1: внешний LLM-вызов с таймаутом 10 секунд и контролируемой ошибкой»
- [F14] `context.md`, строка 24: «OBS-1: логируются только request_id, длительность и статус; содержимое diff/ответа не логируется»
- [F15] `context.md`, строка 9: «Новый endpoint POST /api/reviews принимает payload с полем diff и напрямую передаёт его в ReviewService.review, который формирует prompt из сырого diff и вызывает LLM.generate»
- [F16] `context.md`, строка 10: «Нет проверки длины diff (API-1), нет редактирования секретов (SEC-1), нет таймаута и graceful fallback (REL-1) — риски и потенциальные ошибки/зависания»
- [F17] `context.md`, строка 8: «Инженеры-ревьюеры и авторы PR; вызов может идти от человека или из CI» — в источнике помечено «допущение».

Численных целей нагрузки (rps, длительность, целевая задержка) в источниках нет: только пределы API-1 (413 при > 20 000 символов) и REL-1 (timeout 10 с) и их метрики из problem.md.

### Шаг 2. Результат сопоставления утверждений исходного tests_load.md

| Утверждение исходного tests_load.md | Статус |
|---|---|
| Сценарий «Пиковые обращения к /api/reviews» | подтверждено: [F5] (эндпоинт `POST /api/reviews`), [F15], [F6] (payload с полем diff) |
| Нагрузка «50 rps в течение 60 секунд» | фрагмента нет → допущение, нет в источниках |
| «LLM заменён на быстрый мок (допущение)» | подтверждает только допущение: мок-LLM есть только в метрике SEC-1 [F10]; к нагрузке фрагмента нет → допущение, нет в источниках |
| «Средняя задержка ≤ 500 мс» | фрагмента нет → допущение, нет в источниках (в источниках таймаут LLM 10 с: [F2], [F9]) |
| «Ошибки ≤ 1%» | фрагмента нет → допущение, нет в источниках |
| «Время ответа API и доля 4xx/5xx» | частично подтверждено: время ответа и статус-коды [F9]; доля 4xx/413 по HTTP-логам API [F8]; «доля 5xx» — фрагмента нет → убрано |
| Evidence «Метрики сервера и трейсинг» | противоречит [F4]/[F14] (OBS-1); «метрики сервера» — фрагмента нет → заменено на логи в рамках OBS-1 и источники данных [F8], [F9] |

### Шаг 3. Таблица сценариев из исправленного файла (rag/tests_load.md)

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Пиковые обращения к POST /api/reviews (TRAINING_PR.diff:35) | 50 rps в течение 60 секунд — допущение, нет в источниках; LLM заменён на быстрый мок — допущение, нет в источниках (мок-LLM в источниках упоминается только в метрике SEC-1: problem.md) | HTTP 413 для diff > 20 000 символов (CASE.md:65, API-1; context.md:19); контролируемый ответ при timeout LLM 10 секунд (CASE.md:66, REL-1; context.md:20); средняя задержка ≤ 500 мс и ошибки ≤ 1% — допущение, нет в источниках | Время ответа API и статус-коды (problem.md, метрика REL-1); доля 4xx/413 по HTTP-логам API (problem.md, метрика API-1); доля 5xx — допущение, нет в источниках | Логи в рамках OBS-1: request_id, длительность и статус (CASE.md:70, OBS-1; context.md:24) |
| Контролируемый таймаут внешнего LLM: задержка LLM > 10 секунд (CASE.md:66, REL-1; context.md:20) | Целевое rps и длительность — допущение, нет в источниках | 0% → 100% запросов с задержкой LLM > 10 с завершаются контролируемым ответом ≤ 10 с (problem.md, метрика REL-1; CASE.md:66) | Метрики времени ответа и статус-коды (problem.md, метрика REL-1) | Логи в рамках OBS-1: request_id, длительность и статус (CASE.md:70, OBS-1) |
| Отклонение большого diff: diff > 20 000 символов (CASE.md:65, API-1; context.md:19) | Целевое rps и длительность — допущение, нет в источниках | HTTP 413 для diff > 20 000 символов; 0% → 100% таких запросов получают 413 (CASE.md:65, API-1; problem.md, метрика API-1) | Доля запросов с diff > 20 000 символов, получивших 413 (problem.md, метрика API-1) | HTTP-логи API в рамках OBS-1: request_id, длительность и статус (problem.md, метрика API-1; CASE.md:70, OBS-1) |

## Что изменили в исходном артефакте

- Файл и раздел: `../practice_01/tests_load.md` → полная копия с исправлениями в `rag/tests_load.md`; изменены раздел «Нагрузочные проверки» (таблица) и блок «Как использовали AI».
- Изменение: (1) сценарий уточнён до `POST /api/reviews` со ссылкой на код (TRAINING_PR.diff:35); (2) численные цели 50 rps / 60 с / ≤500 мс / ≤1% помечены «допущение, нет в источниках», вместо них из CASE.md внесены подтверждённые пределы — API-1 (HTTP 413 для diff > 20 000 символов) и REL-1 (timeout 10 с) и добавлены их сценарии; (3) измерения приведены к источникам: время ответа и статус-коды (problem.md, REL-1), доля 413 по HTTP-логам API (problem.md, API-1), «доля 5xx» убрана; (4) evidence ограничен OBS-1 (request_id, длительность, статус), «метрики сервера и трейсинг» убраны; блок «Как использовали AI» ссылается на строку RAG в `../prompts.md`.
- Как проверили ссылки: воспроизводимо — для каждого [F#] выполнить `grep -F "<цитата>" practices/practice_01/<файл>` в корне репозитория и убедиться, что фрагмент найден в указанном файле: `grep -n "diff длиннее 20 000 символов отклоняется с HTTP 413." CASE.md` (строка 65, [F1]); `grep -n "внешний LLM-вызов имеет timeout 10 секунд" CASE.md` (строка 66, [F2]); `grep -n "в лог пишутся только request_id" CASE.md` (строка 70, [F4]); `grep -n "@app.post(\"/api/reviews\")" TRAINING_PR.diff` (строка 35, [F5]) и `grep -n "payload\[\"diff\"\]" TRAINING_PR.diff` (строка 37, [F6]); `grep -n "answer = self.llm.generate(prompt)" TRAINING_PR.diff` (строки 19–22, [F7]); `grep -n "Доля запросов с diff > 20000 символов" problem.md` ([F8]) и `grep -n "Доля запросов с задержкой LLM >10с" problem.md` ([F9]); `grep -n "мок-LLM" problem.md` ([F10]); `grep -n "API-1: diff > 20 000" context.md` (строка 19, [F12]); `grep -n "REL-1: внешний LLM-вызов" context.md` (строка 20, [F13]); `grep -n "OBS-1: логируются только" context.md` (строка 24, [F14]); `grep -n "endpoint POST /api/reviews" context.md` (строка 9, [F15]); `grep -n "Нет проверки длины diff" context.md` (строка 10, [F16]); `grep -n "вызов может идти от человека" context.md` (строка 8, [F17]).
- Что отклонили как неподтверждённое: «50 rps», «в течение 60 секунд», «средняя задержка ≤ 500 мс», «ошибки ≤ 1%» (фрагмента нет — только допущение); «доля 5xx» (фрагмента нет); evidence «метрики сервера и трейсинг» (органичено OBS-1 — в лог только request_id, длительность и статус [F4]/[F14], трейсинг противоречит фрагменту).