# Нагрузочные проверки

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Пиковые обращения к POST /api/reviews (`app/api.py`, `create_review`) | 50 rps в течение 60 секунд (допущение); LLM заменён на быстрый мок (допущение) | 100% ответов 200; p99 ≤ 500 мс (целевое значение, пересмотреть после первого замера) | Статус, длительность запроса | Лог: `status=200` у всех `request_id`, p99 по полю длительности. Запуск: `k6 run load/reviews.js` (допущение: k6) |

Если нагрузочное тестирование пока не требуется, обоснуйте это и назовите условие, после которого оно понадобится.

## Как использовали AI

- Строка Few-shot в [`../prompts.md`](../prompts.md)
- Что проверили и исправили сами: критерии связали с правилами OBS-1 (только статус и длительность в логе), уточнили эндпоинт и метод по `TRAINING_PR.diff`, заменили среднее на p99 и добавили воспроизводимый запуск
