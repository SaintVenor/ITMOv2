# Нагрузочные проверки

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Пиковые обращения к POST /api/reviews: `create_review(payload)` передаёт `payload["diff"]` в `review_service.review`, который вызывает внешний `self.llm.generate(prompt)` (TRAINING_PR.diff:35-37, 19-22); «пиковые» — допущение | 50 rps в течение 60 секунд — допущение, в CASE.md и TRAINING_PR.diff целевого RPS нет; LLM заменён на быстрый мок — допущение (мок-LLM в источниках есть только в метрике SEC-1, problem.md:17); быстрый мок не воспроизводит задержку LLM до 10 с — для неё отдельный сценарий ниже | p99 задержки ≤ 500 мс и доля ошибок ≤ 1% — допущение, таких значений в CASE.md и TRAINING_PR.diff нет; HTTP 413 (API-1, CASE.md:65) и контролируемый ответ при таймауте (REL-1, CASE.md:66) ошибками не считаются | Время ответа API (длительность из лога) и статус-коды; доля 5xx — допущение, в источниках нет | Лог по OBS-1: request_id, длительность, статус (CASE.md:70). Запуск: `k6 run load/reviews.js` (допущение: k6) |
| Контролируемый таймаут внешнего LLM: синхронный вызов в пути запроса с задержкой LLM > 10 секунд (CASE.md:66, REL-1; TRAINING_PR.diff:21; context.md:9) | Целевое rps и длительность — допущение, в источниках нет | 100% запросов с задержкой LLM > 10 с завершаются контролируемым ответом ≤ 10 с (CASE.md:66, REL-1; problem.md:19, целевое 0% → 100%) | Время отклика и статус-коды (CASE.md:66; problem.md:19) | Лог по OBS-1: request_id, длительность, статус (CASE.md:70) |
| Отклонение большого diff: diff длиннее 20 000 символов (CASE.md:65, API-1; context.md:19) | Целевое rps и длительность — допущение, в источниках нет; часть запросов — с diff > 20 000 символов (допущение по доле смеси) | 100% запросов с diff > 20 000 символов получают HTTP 413 (CASE.md:65, API-1; problem.md:18, целевое 0% → 100%) | Статус-код 413 по HTTP-логам (CASE.md:65; problem.md:18) | HTTP-логи API по OBS-1: request_id, длительность, статус (CASE.md:70; problem.md:18, источник данных — «HTTP-логи API») |

Ограничение: в CASE.md и TRAINING_PR.diff нет целевого RPS и SLO для /api/reviews, поэтому численные параметры нагрузки (50 rps, 60 секунд, p99 ≤ 500 мс, доля ошибок ≤ 1%) — допущения; после появления целевых значений или реального трафика их нужно пересчитать.

Если нагрузочное тестирование пока не требуется, обоснуйте это и назовите условие, после которого оно понадобится.

## Как использовали AI

- Строка в [`prompts.md`](prompts.md):
P1-03
- Правки из журнала Практики 2 — [`../practice_02/prompts.md`](../practice_02/prompts.md), строки техник:
  - Few-shot — p99 вместо средней задержки, команда запуска `k6 run load/reviews.js` (допущение: k6);
  - R.C.T.F. — числа без источника помечены «допущение», добавлены сценарии REL-1 (timeout 10 с) и API-1 (HTTP 413), evidence ограничен OBS-1;
  - Chain of Verification — подтверждённые цитатами привязки сценария (TRAINING_PR.diff:35-37, 19-22), «пиковые» как допущение;
  - RAG — к пределам добавлены цели метрик problem.md (0% → 100%) и перекрёстные ссылки на context.md;
  - ReAct — подтверждено, что 50 rps / 60 с / 500 мс / 1% отсутствуют в источниках (только «допущение»); отклонены «трейсинг» и «доля 5xx»;
  - Tree of Thoughts — ограничение про отсутствие целевого RPS/SLO в источниках сохранено одной строкой под таблицей.
- Что проверили и исправили сами: согласовали сценарий с ограничениями таймаута LLM (REL-1) и 413 для больших diff (API-1), вынесли их отдельными строками; каждое число без источника помечено «допущение»; evidence сведено к логу по OBS-1 (request_id, длительность, статус)